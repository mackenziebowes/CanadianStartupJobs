# Database Configuration
# These values match the docker-compose.yml defaults
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=canadian_startup_jobs

# Alternative: Use a connection string instead
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/canadian_startup_jobs
